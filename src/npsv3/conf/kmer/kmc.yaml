kmer_size: 31

kmc_prefix: null

# KMC database of non-unique k-mers in the reference genome (used to filter out non-unique k-mers from the sample).
ref_kmer_counts_kmc_prefix: null 

# Unique k-mer related parameters
max_edges: 5
canonicalize: false

# Sampling related parameters (adapted from Sirén et al. 2024). The total diplotypes should be h(h+1)/2
max_haplotypes: 8
max_diplotypes: 32